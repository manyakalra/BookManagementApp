namespace ConceptArchitect.Finance.Core
{
    public enum Status
    {
        INVALID_ACCOUNT_NUMBER,
        INVALID_CREDENTIALS,
        INSUFFICIENT_BALANCE,
        SUCCESS,
        INVALID_AMOUNT
    }
}